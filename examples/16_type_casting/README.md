# C语言类型转换 / C Type Casting

演示隐式转换、显式转换、指针转换、有符号/无符号转换等。
Demonstrates implicit conversion, explicit casting, pointer casting, signed/unsigned conversion.

## 编译 / Compile
```bash
make
./type_casting
```

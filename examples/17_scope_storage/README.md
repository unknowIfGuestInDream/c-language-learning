# C语言作用域和存储类 / C Scope and Storage Classes

演示auto、static、extern、register存储类和作用域规则。
Demonstrates auto, static, extern, register storage classes and scope rules.

## 编译 / Compile
```bash
make
./scope_storage
```
